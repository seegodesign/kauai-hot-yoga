
  # Kauai Hot Yoga Website

  This project is built on Astro, the javascript web framework for content-driven websites. The content is managed via Sveltia CMS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
